services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - .:/app
    ports:
      - "8001:8001"
    env_file:
      - .env
    command: python manage.py runserver 0.0.0.0:8001
    restart: always
